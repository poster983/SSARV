var assert = require('assert');
var ssarv = require('../index.js');



describe('requiredRoles is an array.  Has a value of: [\"admin\", \"mod\"]', function() {
	context('req userGroup endpoint is an array', function() {
		it('should return true')
	})
});